import 'package:flutter/material.dart';

class MainViewModel extends StatelessWidget {
  const MainViewModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('MainViewModel');
  }
}
