import 'package:flutter/material.dart';

class DetailViewModel extends StatelessWidget {
  const DetailViewModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('DetailViewModel');
  }
}
